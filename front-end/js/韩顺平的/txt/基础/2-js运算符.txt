
和php一样
1、算数运算符：+-*/%，前++--，后++--，左+-，赋值（=）
2、比较运算符：值和类型+地址+值(===/!==)，比较两地址(==/!=)，(><>=<=)，
	"=="：当两边是数值或字符串或混搭，比较内容。当两边是对象比较地址
	"==="：主要比较当两边是数值或字符串或混搭，比较内容和数值类型
3、逻辑运算符：and/&&，or/||，!/not(优先级&&,||,!先逻辑：与或非)
4、三元运算符：表示式1 ? 表达式2:表达式3（如果1为真就取第2表达式的值）
5、字符串运算符：+就是字符串的连接符（这里小心如果是数也会变成字串数据类型的）